# django-weather-app
A full-stack weather application built with Django and Django REST Framework that fetches real-time weather data from a public API and displays it using a clean HTML/CSS interface.
